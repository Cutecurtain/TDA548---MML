// Takes an amount of dice and generates that many d6-throws
    int throwDice(int amountOfDice){
        int diceSum = 0;
        Random rng = new Random();
        
        for(int i = 0; i < amountOfDice; i++){
            diceSum += 1 + rng.nextInt(6);
        }
        
        return diceSum;
    }
